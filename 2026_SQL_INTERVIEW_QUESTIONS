1.FIND THE EMPLOYEES WHO EARN MORE THAN FINANCE DEPARTMENT HIGHEST SALARY
Select * from emp 
where salary>
(select max(salary) from emp where Department='Finanace');

2.FIND 2ND HIGHEST SALARY

SELECT MAX(salary)
FROM table1
WHERE salary < (
    SELECT MAX(salary)
    FROM table1
);

ANOTHE APPROACH

SELECT DISTINCT salary
FROM table1
ORDER BY salary DESC
LIMIT 1
OFFSET 1;

2.FIND 3ND HIGHEST SALARY
SELECT MAX(salary)
FROM employees
WHERE salary < (
    SELECT MAX(salary)
    FROM employees
    WHERE salary < (
        SELECT MAX(salary)
        FROM employees
    )
);

3.find customers who ordered on friday

select dayname(orderdate) from orders where dayname(orderdate)='friday';  here dayname is to convert date to days

4.find employyes who are hired after 1st jan 2024

select employees from table1

where hire_date>'2024-01-01';


5.find employees who are earining same as amit

select *from table1 where salary=(select salary from table1 where emp_name='amit');


6.find number of records without  using count(*)

select count(1) from table1;

      or
select count(-1) from table1;


7.FIND EMPLOYEES WHO ARE NOT WORKING IN HR AND IT

SELECT *FROM TABLE

WHERE DEPT<>'HR' AND DEPT='IT'

      (OR)

SELECT *FROM TABLE

WHERE DEPT NOT IN('HR','IT');

