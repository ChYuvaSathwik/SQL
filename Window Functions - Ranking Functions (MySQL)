Employee Table
ID	Name	Department	Salary
1	John	IT	50000
2	Sam	IT	60000
3	Mike	IT	60000
4	Alice	HR	40000
5	Bob	HR	45000
6	David	Sales	70000
7	Tom	Sales	50000
8	Emma	Sales	70000
9	Kevin	Finance	80000
10	Sophia	Finance	75000
11	Liam	Finance	75000
1. Display the complete details of the employee(s) who earn the highest salary in each department.
SELECT *
FROM (
    SELECT *,
           ROW_NUMBER() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS ranks
    FROM Employee
) AS d
WHERE d.ranks = 1;
2. Display the complete details of the employee(s) who earn the second highest salary in each department.
SELECT *
FROM (
    SELECT *,
           DENSE_RANK() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS ranks
    FROM Employee
) AS d
WHERE d.ranks = 2;
3. Display the complete details of the top 2 highest-paid employees in each department.
SELECT *
FROM (
    SELECT *,
           ROW_NUMBER() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS rn
    FROM Employee
) AS d
WHERE rn IN (1, 2);
4. Display every employee along with their salary rank within their department.

If two employees have the same salary, they should receive the same rank, and the next rank should skip accordingly.

SELECT *
FROM (
    SELECT *,
           RANK() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS rank
    FROM Employee
) AS d;
5. Display every employee along with their dense salary rank within their department.

If two employees have the same salary, they should receive the same rank, but no ranks should be skipped.

SELECT *
FROM (
    SELECT *,
           DENSE_RANK() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS rank
    FROM Employee
) AS d;
6. Display every employee along with a unique row number within their department based on salary in descending order.
SELECT *
FROM (
    SELECT *,
           ROW_NUMBER() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS row_num
    FROM Employee
) AS d;
7. Display the employees whose dense rank is 2 in their department.
SELECT *
FROM (
    SELECT *,
           DENSE_RANK() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS rank
    FROM Employee
) AS d
WHERE rank = 2;
8. Display the employees whose row number is 1 in their department.
SELECT *
FROM (
    SELECT *,
           ROW_NUMBER() OVER (
               PARTITION BY Department
               ORDER BY Salary DESC
           ) AS row_num
    FROM Employee
) AS d
WHERE row_num = 1;
